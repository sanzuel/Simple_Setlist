workflows:
  android-release:
    name: Simple_Setlist Android APK
    max_build_duration: 30

    scripts:
      - name: Build installable APK
        script: |
          chmod +x gradlew
          ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/app-debug.apk